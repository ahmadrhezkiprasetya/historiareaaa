[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — semua route diarahkan ke index.html
# agar refresh / deep link tidak 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
